# how to use

put a zipped system update in `updates\`
- pre kinect needs 1888, decompress that in `\firmware`

open xenia

press guide button (or backspace)

if you get low fps turn off windowscale render in Display > Toggle upscale to window
