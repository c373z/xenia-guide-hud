# Extracted firmware images the analysis scripts read (xam17489.pe, hud17489.pe,
# dash.pe). These come from the user's own console files and must never be
# committed - the research notes and scripts in this directory are the work
# product, the basefiles are not. Recreate by copying the extracts here.
work/
__pycache__/
